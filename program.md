# program.md

> 已由用户确认关键决策，正式进入方案设计阶段。

## 运行模式
- [x] 生产（产出完整文档树 docs/，含时空契约校验）

## 项目目标
做一个 **agent 运行时内核**：内核自身只做三件事——开辟/隔离插件的空间、编排插件的执行时间流、强制校验插件契约与权限。
所有具体能力（LLM 适配器、工具、记忆、规划/编排、提示词、向量库……）全部以**插件**形式挂载到内核，且支持**在不停机的情况下随时被替换（热替换）**。

## 本质定义（已通过时空契约校验）
一个以"插件即能力"为第一原则的 agent 运行时空管家：内核 = 空间隔离 + 时间调度 + 规则校验，插件 = 可被挂载/卸载/热替换的能力单元。

### 时空契约（已确认）
- **空间契约**：每个插件拥有独立 `PluginContext`（自身状态/配置/资源句柄）；内核持有只读共享 `KernelContext`（全局配置、事件总线句柄）。插件间禁止直接内存互访，跨插件通信一律走事件总线。热替换时旧插件状态需"可丢弃"或"可快照迁移"。
- **时间契约**：主执行流为**事件循环 Loop**（`while let Some(event)=bus.recv().await`），插件注册 `on(event)` 回调；agent 主循环本身是状态机（感知→规划→行动→观察），由"编排插件"（本身也是插件）驱动。
- **规则契约**：编译时以 `trait Plugin` + `ApiVersion` 限定接口；运行时加载时校验符号/契约 + 能力声明（capability）。新插件必须满足同版本契约，否则拒绝加载，防止规则穿透致内核崩溃。

## 纯内核边界（已确认 · 本质深化）

内核**只提供原语**，不提供任何业务能力；一切能力皆为插件。

| 维度 | 内核原语（不可插件化） | 非内核职责（全部下沉为插件） |
|---|---|---|
| 空间 | `Registry` 归属表、`PluginContext`/`KernelContext` 边界、快照/迁移协议 | 状态存放、记忆、向量、文件 |
| 时间 | 事件总线、`Scheduler`、`LifecycleState` + drain、背压与优先级 | agent 状态机如何推进、调用哪个模型 |
| 规则 | `ApiVersion` 校验、`Capability` 门控、依赖图环检测、panic 隔离、`trace_id` 串联 | 任何业务规则、策略、提示词 |

- **关键判定**：执行域适配器（`InProcessDomain`/`WasmDomain`/`ProcessDomain`）是**内核的一部分，不是插件**——它们是时空规则的执行者，而非被装载的能力单元。
- **易漏项**：可观测性（跨插件 `trace_id` 串联）必须由内核承担，插件自打日志无法完成链路追踪。

## 三层插件契约（新增 · 结构修正）

"插件接口"不是单一接口，而是三层：

| 层级 | 名称 | 语言相关 | 载体 | 说明 |
|---|---|---|---|---|
| L1 | 语义契约 | 无关 | 文档 + JSON Schema | 身份/能力/事件模型/生命周期/状态/错误/时间语义 |
| L2 | 线协议（ABI） | 无关 | `kernel.wit` / `kernel.proto` / C 头文件 | **真正的插件接口** |
| L3 | 语言绑定 | 有关 | Rust trait / Python ABC / TS interface | 仅是 L2 的一种方言 |

- **问题**：当前 `core::Plugin` 一肩挑三职；Rust 无稳定 ABI，`Box<dyn Plugin>` 跨动态库边界存在 UB 风险 → 跨语言与动态加载均不可行。
- **修正**：`trait Plugin` 降级为 L3 绑定；以 L2 作为唯一契约源，新增 `schema/` 承载。
- **推论**：混合执行域不是"性能 vs 隔离"的权衡，而是**契约层级的分野**——进程内 = 同语言快车道，WASM/进程 = 跨语言通用道。

详见 `docs/05-插件契约与跨语言.md`。

## 目标编程语言（已确认）
- [x] Rust（编译期强校验 + WASM 隔离生态成熟）

## 插件隔离 / 热替换机制（已确认）
- [x] 混合：核心可信插件走进程内 Trait（`Arc<dyn Plugin>`，注册表原子替换），不可信/需强隔离走 WASM 或独立进程；统一抽象为"多执行域（ExecutionDomain）"。

## 首批范围（已确认）
- [x] 完整内核 + 骨架插件：内核全量设计（生命周期/调度器/注册表/多执行域）+ 1~2 个示例插件（LLM 适配器、工具注册）跑通热替换闭环。

## 验证契约（P/Q/I）
- P（前置）：内核 init 后注册表就绪、配置完整、依赖服务可达。
- Q（后置）：热替换后新插件对事件响应正确、旧 in-flight 请求已 drain 完成。
- I（不变量）：每次 `init` 必有对应 `destroy`；插件间无直接内存互访；接口 `ApiVersion` 向后兼容。

## 修改范围
仅新增内核与插件框架代码，不依赖外部未声明服务（除非确认基础设施插件）。

## 约束条件
- 内核不得包含任何具体业务能力（业务必须下沉到插件）。
- 内核原语（注册表/调度/能力门控/域适配器）不得插件化，否则规则可被绕过。
- **插件只依赖 `sdk`，禁止依赖 `kernel` 内部模块**（防规则穿透与编译耦合）。
- 热替换不能引发时间流撕裂（in-flight 必须安全 drain）。
- 任意插件崩溃不得拖垮内核（进程内插件需错误隔离，WASM/进程域天然隔离）。

## 研究方向（优先级）
1. 热替换的零停机与状态迁移方案（最高优先级）。
2. 多执行域（in-process / wasm / process）统一抽象。
3. 插件依赖图与加载顺序/循环依赖检测。

## 待决策项（阻塞跨语言步骤，不阻塞结构重构）
| # | 决策点 | 默认建议 |
|---|---|---|
| D1 | 跨语言先做进程域（gRPC）还是 WASM 域（wit） | 先进程域（对语言最宽容、调试友好） |
| D2 | 进程域传输格式 | protobuf（有 Schema，可生成 stub） |
| D3 | 是否保留 Rust 进程内域 | 保留（同语言零开销快车道） |
| D4 | 插件清单格式 | TOML + JSON Schema 校验 |
